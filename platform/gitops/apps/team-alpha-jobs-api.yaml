apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: team-alpha-jobs-api
  namespace: platform-system
spec:
  project: default
  source:
    repoURL: https://github.com/thunderbolt-ops/ecps.git
    targetRevision: main
    path: apps/team-alpha/jobs-api/k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: team-alpha
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
