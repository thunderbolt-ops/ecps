# Internal Developer Platform (IDP)

This directory contains components that enable self-service
application onboarding for ECPS.

The IDP abstracts platform complexity and provides:
- Standardized service templates
- Opinionated defaults
- Guardrails enforced by the platform

Application teams interact with the platform primarily via the IDP.

