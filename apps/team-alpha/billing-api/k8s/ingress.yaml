apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: billing-api
  namespace: team-alpha
  labels:
    app: billing-api
    team: team-alpha
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: billing.team-alpha.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: billing-api
                port:
                  name: http
